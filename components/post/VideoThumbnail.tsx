import { VideoPost } from '../../../lib/types'

interface Props {
  post: VideoPost
}

const Video = ({ post }: Props) => {
  return (
    <div>
      <h1>Image thumbnail</h1>
    </div>
  )
}

export default Video
